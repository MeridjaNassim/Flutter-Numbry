name: learn_clean_archi
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  # Local cache
  shared_preferences: ^0.5.6+3
  #Service Locator
  get_it: ^4.0.1
  #State  Management with bloc
  flutter_bloc: ^3.2.0
  # Value Equiality
  equatable: ^1.1.1
  #Functionnal programming
  dartz: ^0.8.9
  #Connectivity and remote packages
  data_connection_checker: ^0.3.4
  http: ^0.12.0+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
